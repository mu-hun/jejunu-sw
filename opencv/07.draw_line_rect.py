import numpy as np
import cv2

blue, green, red = 
image = 
image[:] = 

pt1, pt2 =  
pt3, pt4 =
roi = 50, 200, 200, 100

# 직선 그리기
cv2.line()
cv2.line()    			# 

# 사각형 그리기
cv2.rectangle()             # 
cv2.rectangle( )                  # 
cv2.rectangle( )  # 내부 채움

cv2.imshow('Line & Rectangle', image)							# 
cv2.waitKey(0)
cv2.destroyAllWindows()											# 